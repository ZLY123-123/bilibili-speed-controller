# Bilibili Mini Speed Controller

当前版本：`v1.0.0`

一个用于 Bilibili 的 Tampermonkey 用户脚本。

它会在视频页面右上角显示一个小型倍速面板，支持自定义输入、常用倍速快捷选择、即时生效、位置记忆，以及对方向键临时倍速行为的增强。

项目文件：

- [bilibili-mini-speed.user.js](C:\Users\zly\Desktop\tampermonkey\bilibili-mini-speed-controller\bilibili-mini-speed.user.js)
- [README.md](C:\Users\zly\Desktop\tampermonkey\bilibili-mini-speed-controller\README.md)

## 功能概览

- 小型悬浮倍速面板
- 输入后立即生效
- 常用倍速下拉菜单
- 自动记忆常驻倍速
- 自动记忆面板位置
- 增强右方向键原生临时倍速逻辑
- 可选启用长按左方向键临时降速

## 面板说明

面板默认包含以下元素：

- `倍速` 标签
- 一个数字输入框
- 输入框右侧的内嵌下拉按钮
- 一个带文字说明的勾选框：`左键降速`

### 输入框

你可以直接在输入框中输入倍速，例如：

- `1`
- `2.5`
- `4`
- `5.5`

也可以使用数字输入框自带的上下调节。

所有修改都会立即生效，不需要再点确认按钮。

### 常用倍速菜单

点击输入框右侧的小箭头，会弹出常用倍速菜单。

当前预设包含：

- `0.5`
- `0.75`
- `1`
- `1.25`
- `1.5`
- `2`
- `2.5`
- `3`
- `3.5`
- `4`
- `4.5`
- `5`
- `5.5`

点击任意一项后立即应用。

### 左键降速勾选框

面板中有一个文字标注清楚的勾选项：`左键降速`

含义如下：

- 未勾选：不启用脚本提供的左方向键临时降速功能
- 已勾选：启用脚本提供的左方向键临时降速功能

默认状态为：`关闭`

这个开关会被保存，下次打开页面时自动恢复上一次选择。

### 面板拖动

你可以鼠标按住面板空白区域进行拖动。

脚本会自动保存面板位置，下次打开页面时恢复。

## 方向键行为

### 右方向键

右方向键是“基于 Bilibili 原生逻辑的增强”，不是重写。

#### 原生行为

Bilibili 原生播放器长按右方向键时，通常会：

- 经过一个短暂延迟
- 进入原生临时倍速播放状态
- 默认以 `3x` 临时播放
- 松开按键后恢复到原本倍速

#### 脚本增强后的行为

脚本沿用原生播放器的：

- 触发时机
- 临时倍速状态
- 提示流程
- 松开后的恢复流程

脚本只会在原生临时倍速已经触发后，调整临时倍速值。

规则如下：

- 原本播放倍速小于 `3x`：保持原生行为，临时 `3x`
- 原本播放倍速等于 `3x`：临时 `4x`
- 原本播放倍速大于 `3x`：临时“当前倍速 + 1”

例如：

- 常驻 `3x`，长按右方向键时临时 `4x`
- 常驻 `4x`，长按右方向键时临时 `5x`
- 常驻 `5.5x`，长按右方向键时临时 `6.5x`

松开右方向键后，仍然恢复到原来的常驻倍速。

### 左方向键

左方向键不是基于 Bilibili 原生长按左键逻辑增强，而是脚本提供的可选功能。

#### 原生行为

Bilibili 原生播放器中，长按左方向键更接近进度控制行为，不是临时降速播放。

#### 脚本行为

只有当你勾选了面板中的 `左键降速` 后，脚本才会接管左方向键：

- 短按左方向键：回退 `5` 秒
- 长按左方向键：进入临时降速
- 松开左方向键：恢复到原来的常驻倍速

临时降速规则如下：

- 原本播放倍速小于 `3x`：临时降低 `0.5x`
- 原本播放倍速大于等于 `3x`：临时降低 `1x`

例如：

- 常驻 `2x`，长按左方向键时临时 `1.5x`
- 常驻 `2.5x`，长按左方向键时临时 `2x`
- 常驻 `3x`，长按左方向键时临时 `2x`
- 常驻 `4x`，长按左方向键时临时 `3x`

如果没有勾选 `左键降速`，脚本不会启用这套左键逻辑。

## 临时倍速与常驻倍速

无论是：

- 右方向键触发的临时加速
- 左方向键触发的临时降速

都不会污染你的常驻倍速设置。

也就是说，临时状态只在按住期间生效，松开后恢复，保存下来的默认倍速仍然是你平时手动设置的值。

## 适用页面

脚本匹配以下 Bilibili 页面：

- `https://www.bilibili.com/video/*`
- `https://www.bilibili.com/bangumi/play/*`

也就是：

- 普通视频页
- 番剧 / 剧集播放页

## 安装方法

### 前置要求

需要先安装浏览器扩展：

- [Tampermonkey](https://www.tampermonkey.net/)

### 安装脚本

1. 打开 Tampermonkey 控制面板
2. 新建脚本
3. 用 [bilibili-mini-speed.user.js](C:\Users\zly\Desktop\tampermonkey\bilibili-mini-speed-controller\bilibili-mini-speed.user.js) 的内容覆盖默认模板
4. 保存
5. 刷新 Bilibili 视频页面

## 使用方法

### 基本倍速设置

1. 打开任意 Bilibili 视频页
2. 播放视频
3. 在右上角找到小型倍速面板
4. 通过以下任一方式设置倍速：

- 输入框手动输入
- 输入框上下调节
- 右侧箭头打开常用倍速菜单后点击预设值

### 启用左键降速

1. 在面板中找到 `左键降速`
2. 勾选复选框
3. 点击视频区域，确保播放器获得焦点
4. 长按左方向键测试临时降速

### 使用右方向键增强

1. 先设置你想要的常驻倍速
2. 点击视频区域，确保播放器获得焦点
3. 长按右方向键

### 使用左方向键增强

1. 先勾选 `左键降速`
2. 设置你想要的常驻倍速
3. 点击视频区域，确保播放器获得焦点
4. 长按左方向键

## 数据保存

脚本使用浏览器 `localStorage` 保存以下内容：

- 最近一次常驻倍速
- 面板位置
- 左键降速开关状态

对应键名：

- `tm-bilibili-mini-speed-rate`
- `tm-bilibili-mini-speed-position`
- `tm-bilibili-mini-speed-left-hold-enabled`

## 注意事项

### 1. 右方向键增强依赖当前 B 站播放器结构

右方向键增强逻辑依赖 Bilibili 当前原生播放器的临时 `3x` 流程。

如果以后 B 站修改了：

- 长按右键逻辑
- 临时倍速提示 DOM
- 播放器内部状态流

那这一部分可能需要重新适配。

### 2. 左方向键是脚本自定义逻辑

左方向键临时降速不是 Bilibili 原生默认能力，而是脚本可选提供的行为。

### 3. 需要播放器焦点

如果左右方向键没有反应，通常是因为键盘焦点不在播放器环境里。

可以先点击一下视频区域，再进行测试。

### 4. 可编辑区域不会触发快捷逻辑

为了避免干扰输入，脚本在以下区域中不会触发方向键临时倍速逻辑：

- 倍速输入框
- 评论输入框
- 其他文本输入框
- `textarea`
- `contenteditable` 区域

### 5. 倍速范围限制

脚本内部限制倍速范围为：

- 最低 `0.1x`
- 最高 `16x`

## GitHub 版本管理建议

既然你想把版本记录交给 GitHub 管理，推荐这样做：

### 1. README 只保留当前版本说明

README 里只写：

- 当前版本号
- 当前功能
- 当前安装方式
- 当前使用方法

不要在 README 里长期维护完整历史版本列表。

### 2. 使用 Git 提交记录做变更历史

每次修改后写清晰的 commit message，例如：

```bash
git add .
git commit -m "feat: add optional left-key slow mode toggle"
```

### 3. 重要版本打 Tag

例如当前这一版可以打：

```bash
git tag v1.0.0
```

以后发布新版本时，再按你的发布节奏继续增加新的 tag 即可。

### 4. 在 GitHub Releases 写正式更新说明

如果你把仓库推到 GitHub，建议：

- README 只保留当前状态
- 历史更新记录写到 GitHub Releases
- 每个 Release 对应一个 Tag，比如 `v1.0.0`

这样会比把所有历史都堆在 README 里更清晰。

### 5. 最简单的本地初始化流程

如果你还没建仓库，可以在这个项目目录里执行：

```bash
git init
git add .
git commit -m "chore: initial release v1.0.0"
git tag v1.0.0
```

如果之后你要推到 GitHub，再执行：

```bash
git remote add origin <你的仓库地址>
git branch -M main
git push -u origin main
git push origin v1.0.0
```
